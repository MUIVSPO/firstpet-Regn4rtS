# 10 Заданий по JAVA Script


# Задание 1. Базовый вызов - функция без параметров
function sayHello() {
  console.log("Hello, JS!"); // просто выводим текст в консоль
}

# Задание 2. Параметры - функция принимает имя
function greet(name) { // name - параметр функции
  console.log(`Hello, ${name}`); // используем шаблонную строку
}

# Задание 3. Приветствие с проверкой - с условием
function greetWithCheck(name) {
  if (name) { // если name передано (не undefined, не пустая строка и т.д.)
    return `Hello, ${name}`; // возвращаем приветствие с именем
  }
  return "Hello, stranger"; // иначе - приветствие для незнакомца
}

# Задание 4. Возведение в квадрат - математическая операция
function square(x) { // x - число для возведения в квадрат
  return x * x; // умножаем число на само себя
}

# Задание 5. Сумма массива - работа с массивами
function sumArray(arr) { // arr - массив чисел
  let sum = 0; // создаем переменную для суммы, начинаем с 0
  for (let num of arr) { // проходим по каждому элементу массива
    sum += num; // добавляем текущее число к сумме
  }
  return sum; // возвращаем итоговую сумму
}

# Задание 6. Минимум из двух - сравнение чисел
function min(a, b) { // a и b - два числа для сравнения
  if (a < b) { // если a меньше b
    return a; // возвращаем a
  }
  return b; // иначе возвращаем b (если b меньше или они равны)
}

# Задание 7. Стрелочные функции - современный синтаксис
const double = (x) => x * 2; // стрелочная функция в одну строку
// (x) - параметр
// => - "стрелка"
// x * 2 - что возвращаем (автоматически)

# Задание 8. Колбэк - функция, принимающая другую функцию
function apply(fn, x) { // fn - функция, x - число
  return fn(x); // вызываем переданную функцию fn с аргументом x
}

# Задание 9. Фильтр по длине слова - работа со строками и массивами
function filterByLength(words, minLen) { // words - массив слов, minLen - минимальная длина
  return words.filter(word => word.length > minLen); // filter создает новый массив
  // word => word.length > minLen - функция-фильтр
  // оставляет только слова, длина которых > minLen
}

# Задание 10. Работа с массивом - преобразование строк
function toUpperArray(arr) { // arr - массив строк
  return arr.map(word => word.toUpperCase()); // map создает новый массив
  // word => word.toUpperCase() - функция-преобразование
  // для каждого слова вызываем метод toUpperCase()
}
